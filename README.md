# Sample Hardhat Project

This project demonstrates randomness attack and it fixes in solidity.

Try running some of the following tasks:

```shell
npx hardhat help
npx hardhat test
GAS_REPORT=true npx hardhat test
npx hardhat node
npx hardhat run scripts/deploy.js
```
